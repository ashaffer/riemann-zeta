# Canonical RH proxy ledger

> **Frontier notice (2026-09-03):** this remains a logical-carrier index, but
> its 2026-08-08 priority state is historical.  For the live uniform-strip
> decision state, run `python3 src/zeta23_correction_context.py resume` and
> read
> [`ZETA23-SPLIT-CARLEMAN-FORM-DOMAIN-CLOSEOUT-2026-09-03.md`](ZETA23-SPLIT-CARLEMAN-FORM-DOMAIN-CLOSEOUT-2026-09-03.md).
> The post-synthesis decision path is the verified pre-R181 plan snapshot.

Status: canonical logical map, 2026-08-08.  This document does not claim a
proof of the Riemann Hypothesis.

## 1. Purpose

The chronological [`REDUCTION-REGISTRY.md`](REDUCTION-REGISTRY.md) records
what was tried.  It intentionally mixes exact equivalences, sufficient
conditions, consequences of RH or its negation, proof mechanisms, numerical
diagnostics, and rejected mechanisms.  This ledger performs the complementary
job: it deduplicates those entries by their underlying RH carrier and records
the direction of every logical arrow.

There is an important semantic point.  All exact RH criteria are in one
logical equivalence class.  The proxy families below are kept separate only
because they expose different mathematical structures that might provide an
independent proof engine.  Calling two families different does **not** mean
that they are logically or probabilistically independent.

The older [`RH-EQUIVALENCE-ATLAS.md`](RH-EQUIVALENCE-ATLAS.md) remains a
historical mechanism-ranking snapshot.  This file is the source of truth for
current logical status.

For the primary-source theorem floor beneath the R65--R98 branch, use
[`publication/IMPORTED-ANALYTIC-BASELINE.md`](../publication/IMPORTED-ANALYTIC-BASELINE.md).

## 2. Labels and evidence boundary

| Label | Meaning |
|---|---|
| **EQ** | Both implications with RH are established, subject only to the evidence basis named in the row. |
| **EQ-L** | Exact equivalence after the explicitly named consensus-literature inputs are imported. |
| **SUFF** | The premise implies RH; the converse is unproved, false, or would require extra structure. |
| **NEC** | A proved consequence of RH, with no proved converse. |
| **COND** | A conditional implication, commonly a theorem about what an off-line zero would force. |
| **MECH** | A proposed or partial proof mechanism for another proxy; it has no RH implication by itself. |
| **DIAG** | A finite, numerical, or structural diagnostic only. |
| **CLOSED** | The stated proof mechanism has been refuted, made circular, or shown too weak. |

Unbolded words such as “endpoint,” “stronger,” “identity,” and “mixed” are
prose modifiers, not additional status labels.

“Project analytic” below means that a conventional proof is written in this
repository.  It is not a claim of peer review.  Several such proofs use named
standard inputs, especially Guinand--Weil, Suzuki's screw-function identity,
Krein--Langer theory, and first-crossing results.  The chronological registry
and the linked reports state those dependencies.  Lean checks of abstract
order, kernel, or finite-dimensional lemmas do not by themselves establish
the zeta specialization.

## 3. Canonical exact-proxy families

Write

```text
Delta = sup_rho abs(Re(rho)-1/2),
```

where the supremum is over nontrivial zeta zeros with multiplicity ignored.
The following representatives are exact RH proxies at the stated
quantifiers.

| Family | Canonical representative | Status and basis | Related registry rows |
|---|---|---|---|
| **P0: divisor geometry** | `Delta=0`, equivalently every nontrivial zero is on the critical line | **EQ**, definition; Speiser and full real-zero formulations are classical equivalents | R8; full targets in R20, R23, R43 |
| **P1: global Weil form** | `Q_W(f)>=0` for every admissible compactly supported test function | **EQ-L**, classical Weil criterion with the repository's arithmetic normalization | R1, full-square reading of R4, R28, R30, R33--R40, R54 |
| **P2: all-window nondegeneracy** | Every localized completed-form-domain Weil form has trivial radical | **EQ-L**, using small-window positivity, spectral continuity, and the named first-crossing package | R2 and its compressed-kernel form R16 |
| **P3: support-uniform floor stability** | The localized floors admit one support-independent lower bound; equivalently their negative deterioration is subexponential | **EQ-L**, project analytic semiboundedness theorem plus screw/Nevanlinna inputs | R5 endpoint, R56 vanishing-shift clause, R62 |
| **P4: one fixed box discrepancy** | For one fixed `ell>0`, the von Mangoldt discrepancy `D_ell(R)`, its actual-prime version `P_ell(R)`, or any fixed-order centered balanced Vaughan sum of R68 is bounded, equivalently subexponential | **EQ-L**, project analytic consequence of Guinand--Weil, the unconditional prime-power tail, and the exact fixed-order Vaughan decomposition; each exact growth exponent is `Delta` | R65--R86 |
| **P5: compressed prime Gårding endpoint** | `(PG_0)` holds on every `PW_(U/2)`, for every `eta>0`, with a support-independent `C_eta` | **EQ-L**, project analytic reduction using P3 and the explicit formula | R64 endpoint |
| **P6: Herglotz/de Branges/Schur positivity** | Every shifted ratio `Theta_a` is Schur; equivalently the exact completed zeta kernel has the required Herglotz or Hermite--Biehler positivity | **EQ-L**, classical analytic-function theory with normalization audited here | R9, R21, abstract global part of R22; also the signature coordinates R28 and R30 |
| **P7: global entire/operator spectrum** | The nonzero spectrum of the coefficient-defined trace-class companion `K` lies in the positive real axis | **EQ**, project analytic determinant theorem `det(I+wK)=X(w)` | R63; full real-rootedness targets R18, R20, R23, R43 are related carriers |
| **P8: prime/Möbius cancellation** | `M(x)=O_epsilon(x^(1/2+epsilon))` for every `epsilon>0`, or a Riesz or prime-error criterion at its exact classical quantifiers | **EQ-L**, classical criteria | R7; endpoint hidden in R44 and R48--R51 |
| **P9: Li coefficients** | `lambda_n>=0` for every positive integer `n` | **EQ-L**, Li's criterion | R24 |
| **P10: arithmetic extremals** | Nicolas's primorial inequalities, or Robin's inequality at the exact classical threshold | **EQ-L**, classical criteria | R25 and R26 |
| **P11: Nyman closure** | The Nyman--Beurling--Baez-Duarte `L^2` closure criterion | **EQ-L**, classical criterion | R27 |

Thus a compact current equivalence spine is

```text
RH
  <=> P0
  <=> P1 <=> P2 <=> P3
  <=> P4 <=> P5
  <=> P6 <=> P7
  <=> P8 <=> P9 <=> P10 <=> P11.
```

This display records logical equivalence, not a chain of new proofs.  In
particular, P8--P11 are long-known criteria, while P3--P5 and the specific P7
companion are repository reductions whose novelty and proofs still require
specialist review.

## 4. What is genuinely stronger than RH

The following statements must not be promoted as exact proxies merely because
they would prove RH.

| Stronger or one-way target | Why it is not recorded as an exact equivalent |
|---|---|
| A scale-uniform adjacent-support contraction or complete collar propagation package (R3, R11--R15) | It is a sufficient proof certificate.  RH supplies positivity, but no converse construction of the proposed quantitative margins is known. |
| A particular compact-local limit of real-zero finite-window determinants to `xi` (R18, R19, part of R56) | Hurwitz gives the forward implication to RH.  RH does not presently construct this approximating family or its convergence. |
| A positive arithmetic Hodge, adelic colligation, or all-place trace realization (R10, arithmetic part of R22, R45, R46) | Its endpoint positivity is P1 or P6, but the requested arithmetic realization contains additional structure not known to follow from RH. |
| Hodge quotient observability or the strengthened Schur contraction (R41, R42) | The inequality is strictly stronger than Weil positivity and its intended implementation has a stable numerical counterexample. |
| Growing-cutoff shrinking-cylinder recurrence (R52) | It is an open sufficient recurrence theorem; no converse is known and the required uniform small-ball input may already carry RH-strength. |
| A canonical positive invariant metric for a global zeta operator (R53 or a strengthened R63) | Only after the global operator and its zeta spectral dictionary are constructed would the metric imply RH.  The metric also requires diagonalizability or semisimplicity and can therefore impose zero simplicity. |
| A positive logarithmic-energy reserve in R64 | The reserve is stronger than the endpoint P5 and forces an extra little-`o` zero-gap consequence. |
| Pointwise positivity of the R64 multiplier | It is stronger than compressed Paley--Wiener positivity and is false at the required scale. |

## 5. Complete R1--R98 map

The “family” column names the closest canonical endpoint, not a claim that the
row proves that endpoint.

### R1--R22

| ID | Family | Logical role | Canonical disposition |
|---|---|---|---|
| R1 | P1 | **EQ-L** | Canonical global Weil representative. |
| R2 | P2 | **EQ-L** | Exact after the named continuity and first-crossing inputs; not an independent positivity proof. |
| R3 | P2 | **SUFF / MECH** | Finite-product propagation certificate; uniform factors remain open. |
| R4 | P1 | **NEC** as written | The RH-conditional zero-frame square factorization is a coordinate expression; asserted for all tests it becomes P1. |
| R5 | P3, P4 | **COND**, with an **EQ-L** endpoint | An off-line zero forces exponential negative witnesses; subexponential floor deterioration is P3.  R65 later isolates an exact analytic scalar width exponent. |
| R6 | none | **COND**; metamathematical | Finite-witness independence lemma; the analytic-to-arithmetic bridge is absent. |
| R7 | P8 | endpoint **EQ-L**; other forms **CLOSED** or **SUFF** | The exact Mertens criterion is `M(x)=O_epsilon(x^(1/2+epsilon))` for every `epsilon>0`; qualitative Chowla is too weak; a restricted-weight estimate remains only sufficient. |
| R8 | P0 | endpoint **EQ**; spacing route **CLOSED**; occurrence injectivity stronger | Pointwise ordinate-location rigidity is P0; asymptotic spacing is insufficient; injectivity on occurrences also forces simplicity. |
| R9 | P6 | endpoint **EQ-L**; identity **MECH** | Exact zeta Herglotz positivity is P6; the unconditional kernel identity alone has no RH implication. |
| R10 | P1 | **SUFF** | Arithmetic Hodge/trace transfer proposal; its exact positive realization is additional structure. |
| R11 | P2 | **SUFF / MECH** | Full smooth collar package would propagate positivity; the decisive relative estimate is open. |
| R12 | P2 | **MECH / CLOSED** | Exact activation-defect identity, but its symbol has no positive `L^2` floor. |
| R13 | P2 | **SUFF / MECH** | Event-driven propagation; separate low-block positivity failed, coupled control remains open. |
| R14 | P2 | local identity; global **SUFF** | Exact block-Schur certificate at one event; all-event control would reach P1. |
| R15 | P2 | **MECH / DIAG** | Leakage result and finite ratios do not supply the required form-relative contraction. |
| R16 | P2 | **EQ-L** | Best compressed-operator representative of R2; zeta instantiation imports named domain inputs.  The remaining ordinary-`L^2` representative regularity concerns the proposed realization, not this completed-domain iff. |
| R17 | P2 | **COND** | First-crossing radical support saturation under negated RH; no contradiction follows. |
| R18 | P7 | **SUFF** | A specified real-zero determinant limit would prove RH; its existence is stronger and open. |
| R19 | P7 | **SUFF / MECH** | Concrete R18 approximation package; parity shortcut is closed. |
| R20 | P0 | **MECH / CLOSED** | Heat-flow degree repackages the Laguerre collision target and lacks global boundary control. |
| R21 | P6 | **EQ-L** | Shifted-Schur representative; the Loewner amplifier is closed. |
| R22 | P6 | abstract endpoint **EQ-L**; arithmetic realization **SUFF**; factorwise route **CLOSED** | Abstract passive realization is equivalent to Schur; an explicit adelic realization is stronger; factorwise construction failed. |

### R23--R44

| ID | Family | Logical role | Canonical disposition |
|---|---|---|---|
| R23 | P0 | **MECH / CLOSED** | Modular-orbit Gram engine for the heat-flow/Laguerre target failed; the first Laguerre inequality alone is not recorded as an iff. |
| R24 | P9 | **EQ-L**, engines **CLOSED** | Li positivity is exact; Hankel, moment, finite-difference, CND, and Toeplitz shortcuts added no independent sign law. |
| R25 | P10 | **EQ-L**, added order **CLOSED** | Nicolas is exact; the proposed transition order is stronger and conditionally false under Cramér. |
| R26 | P10 | **EQ-L**, concavity **MECH** | Robin is exact; CA-envelope concavity compresses candidates but gives no height bound. |
| R27 | P11 | **EQ-L** | Exact closure and Blaschke detector; no arithmetic mechanism forces the inner factor to be trivial. |
| R28 | P1, P6 | **EQ-L** | Zero negative-square index is Weil/Herglotz positivity in signature coordinates. |
| R29 | P1 | **CLOSED** | Local-conductor index assemblies are non-Fredholm or return ordinary zero counting. |
| R30 | P1, P6 | **EQ-L** | Positive Sonine reflection metric is the same signature invariant as R28. |
| R31 | P1 | **CLOSED** | Placewise Gram polarization cannot pay the required completed endpoint cost. |
| R32 | P1 | identity plus **CLOSED** lift | `mu*log=Lambda` cancels disconnected terms, but the direct positive lift is indefinite. |
| R33 | P1 | **MECH**, conditionally exact endpoint | The desired all-support Poincaré gap is restricted Weil positivity after the stated domain identifications. |
| R34 | P1 | **SUFF**, conditional iff | Boundary Weyl signature implies positivity; the converse requires the index-one and invertibility hypotheses. |
| R35 | P1 | **CLOSED** | Proposed monotone positive prime update is false. |
| R36 | P1 | **MECH** | Exact contraction/generator identity; the missing sharp deficit is P1. |
| R37 | P1 | **CLOSED** | Generic probabilistic gaps lose the sharp constant; an exact gap would restate P1. |
| R38 | P1 | exact rewrite plus failed **MECH** | Unrestricted dual-frame domination is Douglas/Weil positivity; fixed-rank angle engines fail. |
| R39 | P1 | unrestricted route **CLOSED** as circular | Unrestricted completion is P1, while the named restricted channels were pruned. |
| R40 | P1 | full endpoint **EQ-L**; proper subclasses **SUFF** and mostly **CLOSED** | Full cycle capacity is the Weil Schur condition; proper-cycle subclasses are stronger and mostly pruned. |
| R41 | P1 | stronger **SUFF**, then **CLOSED** | Hodge observability is strictly stronger than P1 and failed on the intended numerical model. |
| R42 | P1 | **CLOSED** | High-tail lemma is valid, but the required strengthened low-sector contraction has a stable counterexample. |
| R43 | P0, P7 | full endpoint **EQ**; finite cones **DIAG**; product model **CLOSED** | Full centered-`xi` real-rootedness is P0; finite cones are diagnostics; the independent-spin representation is false. |
| R44 | P8 | generic route **CLOSED**; residual **MECH** | Generic pretentious inverse theorem is false; a Möbius-specific endpoint remains only a possible route. |

### R45--R98

| ID | Family | Logical role | Canonical disposition |
|---|---|---|---|
| R45 | P1 | **SUFF / CLOSED** | Finite cyclotomic trace architectures failed; a genuinely infinite all-place construction remains open. |
| R46 | P1 | **CLOSED / MECH** | Bare ultraproduct transfer is insufficient; every useful enrichment still lacks the sign-controlled bridge. |
| R47 | P0 | fixed-partner route **CLOSED**; diagonal fork open | Fixed-partner exceptional-zero replication failed; no exact proxy was produced. |
| R48 | P8 | stability route **CLOSED**; endpoint tautological | Stability is too weak; the exact root-product control is Mertens in factorized form. |
| R49 | P8 | **MECH** | Matching identity is exact, but after saturation its deficiency is exactly `abs(M(N))`. |
| R50 | P0 | **CLOSED** | Fixed-cutoff conditioned Bagchi recurrence has zero-free limiting support. |
| R51 | P8 | **MECH** | Blocker-forest decomposition leaves the Mertens endpoint unchanged. |
| R52 | P0 | **SUFF** | Growing-cutoff recurrence remains open; no converse is known. |
| R53 | P7 | conditionally **SUFF** after a global spectral dictionary; finite class **CLOSED** | The metric would force P0 and diagonalizability only after a canonical global zeta operator is supplied; the finite equivariant trace architecture failed. |
| R54 | P1 | **EQ-L** endpoint; engine **CLOSED** | Vanishing localized Morse index is P1 in index coordinates; natural Euler loops did not compute it. |
| R55 | P1 | standard route **CLOSED**; transport fork conditional | Strict Mourre positivity is circular on the bad subspace; standard dilation mechanism failed. |
| R56 | P3, P7 | vanishing-shift endpoint **EQ-L**; characteristic limit **SUFF**; unitary shortcut **CLOSED** | Cofinal shifts tending to zero are P3; characteristic convergence is stronger; exact unitary shortcut failed. |
| R57 | P7 | **DIAG** | Fixed-shift finite divisor fit fails on present windows but does not refute a cofinal limit. |
| R58 | P7 | **DIAG** | Exact phase-count formula is a topology gate, not an RH proxy. |
| R59 | P7 | **DIAG**; generic route **CLOSED** | Exact coherence reduction; generic de Branges structure cannot force the desired compact phase growth. |
| R60 | P7 | normalization theorem; shortcut **CLOSED** | Clark normalization shows that fixed shift selects mixed spectrum; no RH equivalence results. |
| R61 | P7 | **NEC** | Under RH, generalized strong resolvent and fixed-core measure convergence hold; there is no converse. |
| R62 | P3 | **EQ-L** | Canonical support-uniform semiboundedness/floor dichotomy. |
| R63 | P7 | **EQ** | Canonical trace-class companion spectrum criterion; finite sections do not inherit it. |
| R64 | P5 | **EQ-L** at endpoint | Positive reserve is stronger; pointwise endpoint is false; compressed endpoint remains open. |
| R65 | P4 | **EQ-L** | Canonical one-fixed-box width spectrometer; the required prime bound remains open. |
| R66 | P4 | actual-prime endpoint **EQ-L**; naive transfer routes **CLOSED** | Quantitative PNT makes the centered prime-power remainder summable on geometric progressions.  The surviving centered actual-prime Birkhoff-sum or completed mixed-prime reflection law remains open. |
| R67 | P4 | naive Selberg transfer **CLOSED**; completed arithmetic gate **OPEN** | The exact causal Riccati--Volterra and reflection factorizations expose an indefinite mixed term and pole-scale forcing; generic positivity, fixed finite-codimension or bounded-rank completion, Möbius inversion, and ordinary functional-equation symmetry do not control it. |
| R68 | P4 | fixed-order centered Type-II endpoint **EQ-L**; tested ambient metric/reflection classes **CLOSED** | The exact actual-prime primitive and fixed-order aggregate Vaughan sum have growth exponent `Delta`; fixed-order smoothing confines the bilinear variables to `x^(1/2+/-epsilon)` for any fixed positive `epsilon`.  Direct reflection is two-sided unbounded, rough Euler updates expand, scalar Doob normalization inserts `1/zeta`, and the scalar filtered isometry has centered-ramp dual norm at least `exp(R/2)R^(3/2)`.  The balanced coefficient series retains every zero pole.  Fixed finite cofactor blocks and the separate Euler bulk/boundary pieces raise a multiplicity-`m` zero pole to order `m+1` and remain unbounded after ordinary centerings; only their full sum restores the simple pole.  Uniform smoothing constants also give a moving-order `x^(1/2+o(1))` arithmetic localization, without a proved RH converse.  Aggregate moving-cutoff near-square Type-II cancellation remains open. |
| R69 | P4 | tested ambient-norm/reflection classes **CLOSED**; aggregate polylog gate **EQ-L** | The logarithmic kernel has Volterra--Hankel singular values proportional to its collar width, so fixed-rank centerings and norm-only estimates do not contract.  Equal-cutoff reflection reinforces a coherent semiprime block of size `x^theta/log x`; only the joint moving `B-Z` jump cancels automatically.  A uniform polylogarithmic bound for the full fixed-order centered aggregate is P4/RH-equivalent and remains open; `sqrt(log x)` is only the fully decorrelated heuristic scale.  An `exp(O_ell(k log k))` Euler bound now gives an unconditional growing-order `x^(1/2+o(1))` arithmetic localization, but no varying-test converse, so that moving proxy is not known RH-equivalent. |
| R70 | P4 | exact cutoff conservation **MECH**; tested algebraic sign/contraction engines **CLOSED** | The outer-product cutoff table, weighted logarithmic derivative, flat cutoff transport, full singular reconstruction, and divisor-collar involution all couple the R68 pieces exactly.  After all modes and cofactors are restored they reduce to Vaughan's identity and `-zeta'/zeta`.  The exterior-square defect of entrywise ramp evaluation changes sign, including after prime-power deletion, so it supplies neither positivity nor coercivity.  The remaining non-fiberwise arithmetic estimate is still the P4 aggregate gate, not a new proxy. |
| R71 | P4 | scale-energy endpoint **EQ-L**; complete coboundary dispersion **MECH**; distinct proportional-bank moving-edge converse **THEOREM in R80** | Squaring only after total-product grouping, Type-I centering, and the scale coboundary yields a positive local Gram form. R80 provides a separate program-level proportional-order bank: every tilted spectrum is compact and attained, and the zero-slope limit recovers `Delta` exactly. It does not prove a converse for the fixed-step single triangular schedule. R81 proves the cofactorwise completed nonzero-mode decomposition and the off-axis reciprocal phase. Its punctured axes, conditional all-cofactor tail, completed net summation, and rational-major-arc power remain red. A fixed power for the bare cofactor tail already implies a fixed strip. |
| R72 | P4 | exact Markov/Ward identities **MECH**; all proposed Ward lifts **CLOSED** | Repeated averaging has an exact nonnegative innovation telescope, while `Lambda log+Lambda*Lambda=mu*log^2` decomposes the near-square semiprime diagonal into a connected term and a cubic factor-ratio anisotropy.  The local bridge, higher cumulants, and the last nonlocal center/unequal-product lift all fail.  Exactly, its global remainder is `K(C_full+E_Y,C_full+E_Y)-D_tail`, so the unknown complete P4 carrier has only been renamed.  Complete finite floating B-spline/Vaughan diagnostics violate every natural sign orientation, and the required Toeplitz twist changes sign.  The indefinite Ward/Wick subtraction remains faithful but supplies no bound; only a new direct cutoff-complete two-shift estimate for the R71 energy survives. |
| R73 | P4 | exact two-frequency reduction **MECH**; sign/leading-one/generic engines **CLOSED**; fixed-saving bound **OPEN** | The finite grouped Vaughan polynomial and rank-two center give an exact cutoff-complete double Fourier formula.  Removing only the atomic diagonal leaves `X_I>=-D_I` with `D_I=exp(o(R))`; thus a subexponential upper bound for `X_I` is the full P4/RH endpoint, not a cheaper off-diagonal lemma.  Complete finite diagnostics reject sign, constant-one diagonal domination, terminal covariance, and low-band pointwise variants.  Generic large-sieve, sectorwise, Hardy/scattering, probabilistic, and graph lifts do not pass the coefficient-and-center gate.  The surviving graduated target `X_I<=exp((1-2eta+o(1))R)` for fixed `0<eta<=1/2` would prove the fixed strip `Delta<=1/2-eta`. |
| R74 | P4 | exact Mobius joint dispersion/RG **MECH**; local contraction engines **CLOSED**; global fixed-saving bound **OPEN** | The coefficient-specific expansion leaves one complete four-variable Mobius--prime form.  Known mean values and modern density/almost-all results give no fixed exponent.  R79 plus the uniform Euler lemma prove the predicted `R^(4/5-o(1))` saving for both the exact-head and frozen explicit-center fields, but the saving remains sublinear.  Finite cutoff changes preserve every zero principal part exactly, while the centered Selberg--Riccati quadratic core has arbitrary off-axis double-pole-null directions.  The remaining fixed-power joint estimate is essentially equivalent to a fixed zero-free strip. |
| R75 | P4 | exact minimum-width extraction **MECH**; completed exceptional-set bound **OPEN** | Bernstein does not give width `1/H_I` from an off-line-sized raw value because it sees the global supremum.  For the compact completed transform, Paley--Wiener width, spectral concentration, and a fixed Sobolev hierarchy force a large-energy carrier to occupy measure `gg 1/H_I` in an explicit growing height window.  The missing every-block arithmetic upper bound is stronger than density-one or fixed-height large-values input. |
| R76 | P4 | full-moment exponent **EQ-L**; phase/Haar and center-annihilator engines **CLOSED**; complete fixed saving **OPEN** | For a continuously translated completed localizer, `limsup log(1+U_p(R))/R=pDelta`; hence uniform full-line `U_4<=exp((2-kappa+o(1))R)` gives the sharp fixed strip `Re(rho)<=1-kappa/4`.  Exact product grouping makes the pair diagonal subexponential, but unresolved near-products leave the `X^2` large-sieve term.  Faithful finite blocks reject independent-phase and collision-aware domination.  The center-annihilator is real-axis elliptic and explicitly invertible, so it is norm-equivalent to the original completed field. |
| R77 | P4 | direct fixed-saving attempt **CLOSED** for the tested cell/filter engines; endpoint **EQ-L / OPEN** | The completed pair-cell ledger has signed within/across interference and nonmonotone merge energy.  A power-saving `Lambda*Lambda` cell variance is already the same fixed strip because `(-zeta'/zeta)^2` has a nonzero double pole at every zero.  Connected Selberg convolution returns the prime-derivative field, and a Bernstein--Walsh theorem forces any exponentially contracting all-carrier shift filter to have linear scale span, whose arithmetic cost restores the critical exponent.  No fixed `kappa` is proved. |
| R78 | P4 | attained-edge recurrence **COND**; generic non-attained recurrence **CLOSED**; zeta-specific need **SUPERSEDED by R80** | An attained outer displacement gives an absolutely convergent Bohr edge and positive lower Banach density. The generic nonattained countermodel remains valid for one fixed detector family, but R80's slope bank compactifies height and recovers the horizontal edge as a zero-temperature limit. Current almost-all theorems still lack the matching fixed power and complete norm. |
| R79 | P4 | full-field and frozen-center subpower upper bounds analytic/literature **THEOREM** | The direct distributional explicit formula, VK boundary, and zero-shell optimization prove `E_I^full<=exp[R-c_hR^(4/5)(log R)^(-3/5)]` on the regular fixed-step schedule. A classical-synthesis periodic-Euler lemma transfers it to the frozen evaluated center. Both have `eta(R)->0`; this fixed-step single schedule still lacks its own converse and a fixed arithmetic gap. R80 supplies a converse only for its distinct proportional-order dyadic bank. |
| R80 | P4 | proportional tilted-edge and zero-temperature **MECH/THEOREM**; completed arithmetic lift **OPEN** | At order `k=lambda R`, the two exact bases compactify height, so the Hilbert-valued block spectrum has an attained radius and positive-density recurrence. `Delta=lim_(lambda->0)lambda log r_h(lambda)`, and an `O(log R)` dyadic bank is RH-complete. A retreated cutoff transfers the theorem to the evaluated rank-two center with superexponential fixed-slope defect. VK gives a fixed tilted power whose gap vanishes as `lambda->0`, exactly recovering VK after height optimization. MRSTT and Wright are candidate inputs; no completion-preserving lift of either theorem to the joint R71 square is proved. |
| R81 | P4 | completed discrepancy measure, direct/finite rational modes, off-axis reciprocal phase, and proportional shell-band identities **MECH/THEOREM**; completed Wright bound **OPEN** | The field is the compact window convolved with `t^(-1/2)(sum Lambda(n)delta_n-dt)`. `Lambda=(-mu log)*1` leaves only nonzero direct lattice modes; the imported finite Ramanujan basis gives exact reduced rationals with polylog coefficient norm and one displayed Mertens zero mode. Grouping the double lattice by `theta=an-bm` produces Wright's inverse phase off axis. Its coupled amplitude, determinant/dual-zero and punctured-axis sectors remain. R82 shows that the finite zero coefficient is gauge-dependent, but its coherent nonzero replacement retains the full carrier. Actual B-spline shells have a universal individual-zero band and nonzero Gamma limit, while fixed rationals carry singular-series and zeta/L-zero carriers. Limiting Ramanujan truncation fails at full prime `L2` scale; absolute cofactor damping optimizes to a gain smaller than its paid zero shift. |
| R82 | P4 | finite all-nonzero Ramanujan gauge and primitive lattice **THEOREM**; Schur/frame/theta shortcuts **CLOSED**; gauge-invariant power **OPEN** | A normalized cloud of prime moduli above the active range uses `c_1+c_p=0` to absorb the finite Mertens zero coefficient into nonzero rational modes while preserving the `O(log^5 Y)` coefficient ledger. Hence the augmented center Schur complement is exactly zero, not coercive. The same cloud synthesizes natural energy from logarithmic norm, ruling out generic all-nonzero-mode frame bounds. Primitive fractions reduce `theta=0` to equal denominators and axes to `q=1`, but exact theta completion reconstructs the original shifted Ramanujan correlation; with its diagonal, the separate native upper-half packet is the centered top-prime covariance. A local good-prime gauge lowers moduli to `H log Y`, while scalar cross-cell recombination loses `Y/H` and current Wright bounds do not accept the coupled tensor. See `FINITE-RAMANUJAN-NULL-GAUGE-GATE.md`. |
| R83 | P4 | quotient/contact, sector recompletion, and primitive beat frame **THEOREM**; null optimization and separate-axis routes **CLOSED**; centered power **OPEN** | Gauge optimization transfers any reduction to an exact Feshbach contact term. Recompletion makes the determinant-zero singular-series fluctuation smoothly polylogarithmic but leaves the completed one-point `Lambda-dt` axis. The Ramanujan block matrix has determinant `Y!`; nevertheless a stable frame moves any length-`Y` vector into primitive square-root-denominator beats with polylog square ledger. Fixed-pair tensors looked dense, but R84 supersedes the generic rank diagnosis for the global common dual. The low beats remain the zero carrier. See `GAUGE-QUOTIENT-SECTOR-RECOMPLETION-GATE.md`. |
| R84 | P4 | common-dual ratio tensor and mask-free shift-product grouping **THEOREM/MECH**; generic rank, direct alignment, and high-band gauge shortcuts **CLOSED**; primitive mask and coefficient-aligned centered power **OPEN** | The global canonical coefficients have the special form `W(theta/(pr))`, so log-Mellin separation fits beneath the nominal Wright gain on a sufficiently narrow determinant band. In the original cofactor expansion the R81 amplitude also separates after grouping `k=j theta`, with only divisor loss; the finite primitive basis retains a fractional-shift mask. The canonical coefficient is attached to the slow additive beat, not the reciprocal phase carrying the native product coefficient. Reciprocity leaves their full mismatch, and exact low-band matching forces an ill-conditioned high-beat completion. A derived affine theta counterterm is the remaining concrete subproblem. See `COEFFICIENT-SPECIFIC-LOW-BEAT-TENSOR-GATE.md`. |
| R85 | P4 | natural-mean reciprocal projection and affine Ward/null-gauge algebra **THEOREM**; nonresonant oscillatory component conditionally **FIXED-POWER**; full centered power **OPEN** | The correct inverse-phase center is `c_r(k)/(r-1)`, not one. Existing Kloosterman-fraction theorems give the mean-zero nonresonant component a nominal `H^(-1/40)` saving once the R84 kernel ledger is supplied, but the complementary projection is `mu_r(k)h_p conjugate(h_r)-gamma`, which retains the complete canonical carrier. Reciprocal derivative transfer makes the nonconstant mismatch Wright-compatible without changing that projection. The only exact affine null cloud lives at top-prime scale and costs `Y/T`; at square-root scale its defect is the original Type-II tensor. Thus no strip follows, and the remaining joint estimate is still P4/fixed-strip strength. See `NATURAL-MEAN-AFFINE-FIXED-POWER-GATE.md`. |
| R86 | P4 | signed reciprocal-compression and zero-orbit algebra **THEOREM**; natural square-root-frame contraction **CLOSED**; complete R71 kernel pairing **OPEN** | The exact signed field is `AR_jd-v`, with shifted kernel `AR_jA^*(t,m)=sum_(p!=r)c_p(t-m)c_r(t-m-j)`. The reciprocal transport has no zero shift character, so the zero orbit of `R_j-I` is the full `-gamma` carrier. Canonical whitening gives a dissipative phase-defect square plus Schur leakage, not a small norm. Complete or long flat-shift averaging leaves `-v`, while positive native prime weights give a uniform full-scale residual at prime target points. Hence the missing estimate cannot be a Hilbert contraction of the square-root block. Only the actual all-sector R71 kernel pairing can still cancel, and that remains P4/fixed-strip strength. See `SIGNED-JOINT-RECIPROCAL-COMPRESSION-GATE.md`. |
| R87 | P4 | full reciprocal-response and sector-conservation **THEOREM**; free recompletion route **CLOSED**; original fixed-power energy **OPEN** | Exact CRT/fiber Poisson summation, primitive masks, axes, B-spline seams, and Type I reconstruct the original prime-minus-continuum quadratic energy. The reciprocal response is power-small relative to the nonzero slow-axis response in an explicit growing band, so hidden seam conversion is unavailable. |
| R88 | P0, P4 | positivity/dilation/window mechanisms **CLOSED**; target-conditioned cross-dilate endpoint **OPEN** | Conductor cancellation necessarily creates wrong-sign harmonic mass; differentiated and positive-dilation cones retain the closer pole; an unrestricted modulation bank is false by phase alignment. The only survivor is a zeta-specific correlation conditioned on the tested height carrying a near-one zero. |
| R89 | P0, P4, P8 | named derivative, discriminant-contact, and bounded-arity-flow mechanisms **CLOSED** | Turan resolution plus pole retention gives only `1-beta=O(1/log T)`. The quadratic square locus has zero one-axis coefficient. Positive-density smooth squarefree vertices isolate every fixed-arity power-local flow. These are mechanism no-gos, not evidence for zeros approaching one. |
| R90 | P0, P4, P8 | Newton fixed-power endpoint **EQ-L**; q-free equivalence **EQ**; tempered interpolation and harmonic cascade **CLOSED**; global signed endpoints **OPEN** | `c_k=O(k^(-1/2-delta))` yields the strip `Re rho<=1-2delta`, while q-free transforms retain exactly the zeta divisor. Muntz quadrature annihilates each zero mode; harmonic positivity can be paid by critical-line mass; stable prime-support interpolation costs power-sized conditioning. Growing-arity smooth/rough cancellation and a superoscillatory signed remainder remain open. |
| R91 | P0, P4, P8 | positive-vector and superoscillatory detectors **MECH**; normally convergent scalarization and separate smooth/rough engines **CLOSED**; joint signed strip endpoints **OPEN** | The eta/modulus pair has common zeros exactly at zeta zeros, but every normally convergent pole-cancelling scalar Dirichlet multiplier has artificial recurrent zeros approaching one. Arbitrary prime-gap superoscillation can cancel the pole and retain the target, after which exact explicit-formula restitution leaves the full signed zero remainder. |
| R92 | P0, P4 | continuous Pareto carrier **THEOREM / MECH**; mod-6 and finite-multigap engines **CLOSED**; sawtooth nonvanishing **OPEN** | A genuinely continuous dilation spectrum evades discrete recurrence and yields a bounded nonnegative Mellin density with transform `(s-1)zeta(s)/[s(s+1)]`, so it has no fake zeros. Matrix spectators and finite density filters either miss zeta zeros or restore the complete signed remainder. |
| R93 | P0, P4 | complementary and smooth positive carriers **MECH**; generic positivity, finite smoothness, PF/infinite-divisibility, and ordinary transfer iteration **CLOSED**; arithmetic cell correlation **OPEN** | Off-line zeros are common zeros of two positive complementary tilts. Higher Pareto differences are positive and arbitrarily finitely smooth, but retain the integer comb at the first unsmoothed derivative and hence the abscissa-one variation wall. The Luroth/Levy formulation exposes the surviving signed prime-atom versus pole-field estimate. |
| R94 | P0, P4 | forward Luroth, reciprocal-recurrence, and Mellin-transport mechanisms **THEOREM**; generic transfer/reversal, uniform Mobius-tail, and positive-transport engines **CLOSED**; signed resonant head/queue endpoints **OPEN** | Mixing makes only later rational spectators nonzero, while the first `O(t^2)` Luroth digits retain the target cancellation. Prime-phase recurrence of finite Mobius heads forces an order-one conditional tail and a nonnormal reciprocal-zeta translate family. Phase-aware positive transport has convergence threshold exactly `sup Re(rho)` by radial projection, so only signed scale-global cancellation remains. |
| R95 | P0, P4 | linear Luroth alias and signed nonlattice-filter mechanisms **THEOREM**; finite alias locking, flat/coercive cycle engines, and Puglisi's claimed implication **CLOSED**; one-sided ramp endpoint **OPEN** | The unresolved Luroth head is linear in height and its stationary aliases carry complementary Dirichlet phases. The exponential scale filter is faithful at every zero and reduces a fixed strip to either one side of one bounded signed von Mangoldt ramp. The audited preprint's final alternating-exponential inequality is false in its own cutoff regime, so it cannot be imported. |
| R96 | P0, P4, P8 | global alias, exceptional-character, and one-sided Landau mechanisms **THEOREM**; iterated Poisson, `B^2` zero-copying, and generic ramp engines **CLOSED**; local exceptional-phase or coefficient-specific ramp power **OPEN** | Global Luroth aliases recombine to exactly `zeta(1-s)` and Poisson closes a neutral functional-equation cycle. Mobius `B^2` recurrence is genuine, but finite-prime conditioning selects Euler order, whose value below one differs by exactly `-1/zeta(sigma)` from the hypothetical natural-order value; explicit boundary-layer spikes defeat local normality. Either one-sided `O(x^a)`, `a<1`, control of the signed ramp would prove a strip, but positivity, lcm/divisibility, complementary pairing, scale averaging, and Selberg do not supply it. |
| R97 | P0, P8 | Fredholm/prime-Fock and nonlinear/twist mechanisms **CLOSED**; signed relative first-trace endpoint **OPEN** | Hilbert--Schmidt regularization deletes exactly the first prime trace carrying the reciprocal divisor, and that trace is nonclosable in the `S_2` topology. Lowering scalar coefficient thresholds deletes the zero at one or creates recurrent artificial zeros; zero-preserving scalar germs retain squarefree almost-prime layers and the half-unit barrier. A zero-preserving unimodular multiplicative twist is power-pretentious and only multiplies `1/zeta` by a normal nonzero Euler factor. |
| R98 | P0, P4, P8 | quasi-RH conditional bootstrap audit **CLOSED** for the imported mechanisms; per-zero signed/replication endpoint **OPEN** | Assuming `Theta<1`, global PNT/Mertens exponents and their converses return exactly `Theta`; reciprocal mollifiers decay only to its right. Density, moments, and repulsion permit a sparse exceptional sequence, Turan contradicts only `beta>Theta`, and the functional equation preserves the band. Thus the audited toolbox has `f(Theta)=Theta`, not a strict bootstrap. |
| R99--R103 | P0, P4, P8 | renewal, singular-trace, integrality, ramp-sign, center-annihilation, and sign-spectrometer mechanisms **THEOREM/CLOSED**; transformed off-axis power **OPEN** | Positive renewal and regularized prime traces cannot retain the first reciprocal-zeta trace below abscissa one; the factorial ramp has Littlewood oscillations of both signs. `Q_h` is a valid zero-faithful center annihilator, and the variable-cutoff sign transition has exponent exactly `Theta`, but neither fact supplies its fixed-power bound. |
| R104--R106 | P4, P8 | finite cofactor and dilation-sieve mechanisms **THEOREM**; scalar imported-bound and full-Euler collapse routes **CLOSED** | Continuum annihilation makes the rational cofactor expansion finite and clears its axes, while Vaughan recombination leaves the canonical `C=-mu log` coefficient. Full Euler-product sieving has an incompatible shift span; R123 later repairs that span without changing the endpoint. |
| R107--R119 | P4 | trace-collision and sparse Vieta arithmetic **THEOREM/MECH**; native completed composite bridge **OPEN** | R110 proves a real half-power weighted trace-energy saving, R118 proves the strongest sparse varying-prime endpoint, and R119 gives a pointwise counterpart. The exact R71/Vaughan packet has a composite modulus, full-Fourier/inverse-image supports, and an endpoint translated coordinate; R121 shows that the proposed CRT bridge spends the gain. These are arithmetic advances, not yet a P4 bound. |
| R120--R121 | P4, P8 | primitive inverse-Poisson/Mellin dual **THEOREM**; generic Gram-positivity and CRT-factorization shortcuts **CLOSED**; actual primitive sign **OPEN** | The primitive class has a low-rank Mellin representation in smooth `-mu log` polynomials and signed coprimality coefficients. Its low mode is not annihilated; R124 proves a positive narrow band but only numerically samples full-shell sign change. The balanced composite CRT factorization has square-root factorization cost. Known VK input remains subpower. |
| R122, R126--R127 | P0 | individual-zero replication criteria **COND/THEOREM**; standard universality, punctured-contour, and direct mixed-contour closures **CLOSED at known quantitative strength** | Positive-density self-replication of a near-one zero would contradict zero density and yield a strip. Nonvanishing-target topology forces a missing winding transition; its sharp approximation degree is `Theta(ell^(-2))`, while known recurrence densities are far below the all-moment derivative exceptional set. Moving most of the contour right of one produces the same imbalance. |
| R123, R125 | P4, P8 | short-span free-cofactor collapse **THEOREM**; adjacent prime-packet extraction **CLOSED** | A truncated integer-Mobius operator collapses every free cofactor with span `log M` and a two-cutoff bank retains each zero. Adjacent differences only move the original field to a smaller scale: reciprocal inversion restores the same `-mu log` denominator and the prime slice exhausts the sparse-frame gain. |
| R124, R128 | P4 | primitive integer-ratio/narrow-band positivity and complete all-conductor chirp duality **THEOREM**; full-shell primitive sign change **D-RATED NUMERICAL**; isolated positive-energy adapter **NOT PROVED** | Recombining every conductor class leaves the original positive R71 energy with coefficient `-mu log`. Its centered sum is exactly the inverse Mobius floor transform of the PNT error and has the same power exponent as `M`; refolding and generic reciprocal weak-tail bounds do not bound it. The isolated packet is not proved order-reflecting, but actual spectral indefiniteness has not been interval-certified. |
| R129 | P0, P8 | sigma-differenced logarithmic-derivative positivity **THEOREM**; automatic gamma-cancellation route **CLOSED** | Forward differences give nonnegative prime coefficients and a genuine target-over-pole window while deleting the leading gamma `log T`. The corresponding zero kernel has exactly zero vertical mass and unavoidable negative lobes; actual zero density contributes `gg log T`, and sufficient localization recovers only the classical `1/log T` scale. A target-conditioned signed zero correlation remains open. |
| R130 | P4, P8 | null-tail tight frames and coherent dilation renormalization **THEOREM**; detector-frame surplus **CLOSED** | Null dilations can improve the unnormalized zero multiplier while leaving the frozen Vaughan tail unchanged, but the identical tight-frame law governs their arithmetic energy. A coherent block is stronger pointwise yet equals a smoothed copy of the full center-annihilated prime field at scale `X/M`, plus a power-small discretization error. The fixed-power target is therefore unchanged. |
| R131--R132 | P0 | functional-symmetry and paired-kernel mechanisms **THEOREM/CLOSED**; localized signed recurrence **OPEN** | Reflection closes only an unlocalized symmetric contour; restoring real-part localization reconstructs the original closed zero-bearing cycle. On the logarithmic-derivative side, every nonzero nonnegative functional pair has positive vertical mass and hence a `log T` gamma debt, while exact gamma cancellation forces signed lobes. |
| R133 | P0, P8 | high-jet replication **COND/THEOREM**; torus-volume, ordinary-moment, and natural-Haar engines **CLOSED**; exceptional arithmetic recurrence **OPEN** | High jets bypass winding and would contradict fixed-strip zero density if they recurred early enough. Their guaranteed Euler-phase recurrence rate exceeds the Cauchy localization rate by the exact inequality `r log((r+eta)/(r+delta_*))<=eta-delta_*<1`; this does not rule out an exceptionally early actual prime-log orbit. |
| R134 | P8 | near-semiprime parity geometry **THEOREM**; background-independent recognizable codebooks **CLOSED**; guarded/growing-arity transport **OPEN** | Power-close `p <-> qr` replacements exist, but raw two-sided recognizability collapses every overlap component and leaves a positive-density unsigned exceptional set. Stateful allocation still has to control the majority residual rather than rename `abs(M(N))`. |
| R135 | P0, P1 | exact prime/pole/gamma-null explicit-formula carrier **THEOREM**; absolute, PSD-matrix, and finite-Blaschke closures **CLOSED**; signed alias estimate **OPEN** | Pointwise arithmetic nulling is possible, but its forced high-frequency chirps carry collateral zero mass larger than the target in `L1` and exactly at target scale in `L2`. Aggregate nulling is cheap only after moving the unresolved sign to the zero side. |
| R136 | P0, P8 | arithmetic Nyman endpoint and spike-spreading equivalence **THEOREM**; coefficient-free dilation/floor-operator self-improvement **CLOSED**; Mobius-specific power saving **OPEN** | PNT gives `M(1/x) in L1(0,1)`, and any fixed `Lp`, `p>1`, would prove a fixed strip. Unit increments show that reciprocal weak-`Lq`, `q>1`, already forces `M(N)=O(N^(2/(q+1)))`; conversely any pointwise fixed power saving gives a weak exponent. Thus the weak-tail target is an alternate distributional form, not a qualitatively weaker sparse-exception route. Exact reciprocal scaling and global centering still do not bootstrap the floor recurrence above weak `L1`. |
| R137 | P0, P8 | high-jet resonance entropy **THEOREM**; Haar/diagonal resonators **CLOSED**; exceptional finite-height arithmetic **OPEN** | Any resonator whose main term is a Haar or diagonal average pays an inverse-volume exponent strictly larger than the Cauchy localization exponent for `eta<1/2`. This does not upper-bound a genuinely anomalous actual prime-log orbit or an off-diagonal finite-height main term. |
| R138 | P0, P8 | degree-zero positive quadratic virtual detector **THEOREM**; standard character-selection engines **CLOSED**; exceptional low-conductor interpolation **OPEN** | `zeta L_(chi psi)/(L_chi L_psi)` cancels Gamma and conductor exactly while retaining a nonnegative prime logarithmic derivative. The unresolved payment is a signed cloud of auxiliary `L`-zeros: forcing it to be local requires a Frobenius mask whose weighted conductor lies below the Cauchy budget, which CRT, complement interpolation, and Haar-family averaging do not achieve. |
| R139 | P0, P8 | one-sided quadratic pair replication **COND/THEOREM**; ordinary universality closure **CLOSED** | Reflection turns upper-half constant approximation into a full divisor identity. Real-character zero density bounds its possible pair density by `Q^[-Delta(sigma)+o(1)]` for every `sigma>1/2`; crossing that threshold would prove the corresponding zero-free region. Standard quadratic target support cannot cross it because the needed target is zero-bearing on the forced symmetric interior; opening the contour restores the R126 derivative debt. |
| R140 | P0, P8 | degree-zero localization and Frobenius-mask dichotomy **THEOREM**; general analytic and ordinary-`L2` closures **CLOSED** | The quotient has `>>T log T` uncancelled zeros and poles, but broad positivity/functional-equation/boundary data cannot place a pole near a selected right-edge zero. Cheap missing-class masks force auxiliary zeros at the same exponent and are sparse rather than absent. The all-even-character interpolant cancels the head deterministically, while its Fourier square-root rate strictly exceeds Cauchy localization. Only an exact Boolean signed-divisor theorem or individual polylogarithmic least-Frobenius input remains. |
| R141 | P0, P8 | nonlinear divisor-order separator and regular/permutation Frobenius masks **THEOREM**; one-sign quadratic, ordinary tensor, rank, tower, and exceptional-zero closures **CLOSED** | Reciprocal curvature separates a shared zeta zero from a simple auxiliary zero by one pole order, but an exact calibration theorem forces mixed prime/Type-II coefficients. A Galois regular mask turns the arithmetic cost into `log rd(E)` but needs a sub-square-root split field. A non-Galois permutation mask replaces splitting by Frobenius elements with exponentially small support; its controlled-discriminant monomial realization is open. Dedekind GRH predicts a square-root obstruction for both. |
| R142 | P0, P8 | growing common-pole tensor and derivative common-divisor detector **THEOREM**; absolute tensor/scalar exterior closures **CLOSED** | Pole multiplicity does not change the exponent: its composition binomial is paid identically by remote factors, and regular Laurent terms exponentially condition the isolated top coefficient. Positive differentiated products retain common zeros but not the unfavorable poles or the prime half-threshold. A scalar holomorphic common-zero test necessarily adds a hypersurface divisor; a vector test is the unresolved corona/joint-nonvanishing statement. |
| R143 | P0, P8 | solvable low-support quotient and primitive classification **THEOREM/IMPORTED**; solvable permutation escape **CLOSED** | A sharp block-chain argument turns support `L` in every solvable transitive degree-`m` action into an exact killed quotient of degree at least `m/(2L)`; the number-field quotient has all selected primes split and no larger root discriminant. Primitive sublinear support is Cameron type, and fixed absolute support leaves natural `A_m/S_m`. Thus only a genuinely nonsolvable primitive atom survives. |
| R144 | P0, P8 | exact bounded-support virtual-character mask **THEOREM**; genuine exact representation mask **IMPOSSIBLE** | The class function `(m-Fix)_q` is a nonnegative integral virtual character, deletes every Frobenius of support `<q` at all powers, and has constant normalized divisor/conductor variation for fixed `q`. Sign and two-set corrections optimally kill the natural transposition/`3`-cycle classes. Since those classes generate `S_m/A_m`, a nontrivial genuine representation cannot kill them; exact deletion necessarily creates a signed quotient divisor. The live gate is cheap prescribed Frobenius plus local divisor noncancellation, not the old exponentially small support fraction. |
| R145 | P0, P8 | full-strip-positive explicit-formula and Artin-order closures **CLOSED**; reciprocal Euler-tail closure **THEOREM**; target divisor correlation **OPEN** | Whole-strip nonnegative zero response forces `sech(x/2)` decay and exactly the unconditional `1/p` Poitou scale. Degree-zero nonnegative masks have anti-effective conductor, while the algebraically admissible order character `e*reg` is a common null direction for every exact mask, even under all permutation/Dedekind holomorphy inequalities. Reciprocal jets share the deleted head and their signed coefficients are dominated by direct positive jets, but an uncontrolled regular Laurent germ can flatten every finite local jet without removing the divisor. |
| R146 | P0, P8 | high-density and ramified Artin masks **THEOREM**; entropy and inertia-adjoint closures **CLOSED** | For `m>=3`, the exterior Euler character `det(1-Std)` is a positive degree-zero exact detector supported only on full cycles and annihilates every transposition-ramified factor with signed conductor one. Its absolute degree/conductor costs are exponential. Universally, class-support rarity and absolute Artin complexity obey `C[-log(1-alpha)]>=1`. Exact inertia deletion lies in an invariants kernel whose adjoint contains a genuine permutation order character with zeta multiplicity one, providing a common divisor-null direction even for finite bounded-support banks. |
| R147 | P0, P8 | exterior-cycle cubic selection and auxiliary zero avoidance **THEOREM**; generic family closure **CLOSED** | Current cubic local counts plus Artin/Dirichlet zero density construct no-inert-through-`X` fields with both auxiliary factors zero-free in a prescribed thin rectangle, but only with `log D_K>(2+o(1))X`. The high-jet theorem needs `log D_K=o(X^kappa)`, `kappa<1/2`; even ideal family entropy costs `X/log X`. Family averaging retains a deterministic truncated-zeta regular part, so remote divisors cannot be assigned an unsupported square-root saving. Only an exceptional subentropy field or centered signed divisor theorem remains. |
| R148 | P0, P8 | cubic growing-local-specification and standard-Artin zero avoidance **THEOREM**; absolute Cauchy closure **CLOSED** | For every fixed `sigma>51/56` and bounded height rectangle, BTT local counting plus BTZ log-free density selects a totally real odd-squarefree cubic field with no inert prime through `H`, `log D_K=(2+eta)H`, and zero-free standard denominator. The sign numerator cannot cancel the source divisor, but its other varying zeros still need avoidance or joint control for a uniform Turan lower bound. R147 can impose that avoidance at the same scale. The scale `log D_K asymp H` remains incompatible with the linear criterion `o(H^kappa)`, `kappa<1/2`. |
| R149 | P0, P8 | degree-zero L-data and cubic distinct-divisor imports **THEOREM**; horizontal localization **OPEN** | Nontrivial exterior quotients necessarily have infinitely many zeros and poles; cubically, both residual orientations have `>>T log T` mass and infinitely many zeta zeros specifically survive `L(s,Std)`. These theorems do not place a survivor right of `1/2`. The regular order pattern costs multiplicity `e|G|` in `zeta_E`, but known unconditional multiplicity and Heilbronn-character bounds permit it in the varying-field/height regime. |
| R150 | P0, P8 | translated-carrier Poisson cancellation, deterministic centered common mode, optimal absolute filter, and functional-equation reflection **THEOREM**; absolute symmetric closure **CLOSED** | A normalized positive head tail is a Laplace transform. Multiplying a target pole by it replaces the isolated high jet by an exact Poisson lower-tail factor, exponentially suppressing the target in the head-deletion regime. The same loss appears as left-boundary growth exceeding every Cauchy reserve. Conditional on the common pole and deleted head, every remote regular part cancels the pole jet to exponential accuracy, so convex averaging cannot center it and signed centering has exponential condition number. Pure highest order is the minimax absolute differential filter. Reflection obeys `H(d)H(-d)>=1`; degree-zero symmetry and `3-4-1` restore the adverse unsigned `O(log D)` denominator ledger. |
| R151 | P0, P8 | unramified class-character positive detector and head quotient **THEOREM**; bank zero-avoidance **CONDITIONAL**; orthogonality conductor gain **CLOSED** | Every nontrivial ordinary class character of a real quadratic field gives `zeta_F/L_F(eta)`, an exact degree-zero, conductor-one positive quotient. Its head through `H` vanishes iff `eta` factors through `C_H=Cl(F)/Gamma_H`; the cubic exterior mask is the order-three case, and its least inert prime is the least ideal detected by the character. Minkowski permits the needed anomaly, while GRH-scale `P(eta)<<log^2 D` would rule it out for `kappa<1/2`; no unconditional version is known. Hecke zero density selects a good denominator only for a large head quotient, the rank-one `C_3` case survives, and the normalized class-field bank still pays `asymp log D`. |
| R152 | P0, P8 | exact genus/Redei head bank and elementary-2 mask ledger **THEOREM**; generic genus closure **CLOSED** | Redei theory makes the ramified constraints explicit and cheap, but every ordinary genus detector is exactly the old R138 biquadratic quotient. The external head still asks one density-`1/4` Frobenius class to be absent, and the general elementary-`2` complement has the same strict square-root Fourier cost. Only an exceptional least-class-prime sequence survives unconditionally; GRH predicts it cannot meet the localization scale. |
| R153 | P0, P8 | nonlinear `H -> H^q` support amplifier and known-scale formal implication **THEOREM**; scalar/differential/positive-kernel and zero-free-selection workarounds **CLOSED**; signed forced-`a`-point estimate **OPEN** | The log derivative of `1-(F-1)^q` has support beyond `H^q` but only `O(q log D)` divisor cost, enough under a formal avoidance premise at `log D asymp H`. Algebra forces `q-1` non-Euler mixed factors `N-(1+omega)D`, and R155 proves their simultaneous target-disc avoidance impossible along a retained zero. Favorable residues can cancel the target for arbitrarily long varying Turan blocks; growing `q` forces these values near the target, while rational elimination, entire transforms, differential powers, and globally positive kernels repay the gain through new divisors, high-order outer growth, or a `1/z` tail. |
| R154 | P0, P8 | coprime nonlinear packet and exact resultant elimination **THEOREM**; scalar/vector algebraic escape **CLOSED** | Consecutive root-of-unity filters have only `F=0` in common, but exact elimination gives `(1-F)^qF'/F=F'/F+dR_q(F)/ds`. The support gain is carried entirely by a regular polynomial germ and order-`q` growth at `F=infinity`. Rational common-divisor scalars reduce to monomials, unweighted contours erase the correction, weighted contours pay it on the boundary, and an affine packet has exponentially small fixed-radius vector energy despite the common target pole. |
| R155 | P0, P8 | fixed- and one-value mixed-point inevitability plus truncated-zeta abundance **THEOREM**; zero-free mixed-factor selection **CLOSED** | Head deletion gives convergence to `1` on a right cap. Two omitted values make the quotient family normal and contradict a retained zero. For the single value `2`, the lift `Log(2-F)` meets a fixed lattice only at zeros; a bounded zero divisor again forces two omitted values. Thus useful nonlinear filters must carry mixed points. The deterministic truncated-zeta skeleton has characteristic and summed three-value reduced count `gg H^alpha/(log H)^(3/2)` with radius slack, so typical family moments preserve the abundance. Only a genuinely signed joint reciprocal-power estimate can continue this branch. |
| R156 | P0, P8 | moving-value conservation and cutoff-complete same-sign cancellation **THEOREM**; absolute moving-node and scalar-topology closures **CLOSED** | Nodes collapsing toward `F=1` lose Euler powers; omitted nodes escaping to infinity must grow like a negative power of the head error and reproduce that power in denominator-pole residues. The resulting localization inequalities reduce to a geometry already false for R153. Separately, a finite `O(H)`-degree polynomial with one fixed zero and sub-`H` outer characteristic has forced `F=2` points whose positive residues cancel the target exponentially throughout `Theta(log H)` high orders. Winding and cover monodromy see only logarithmic degree plus an exact boundary germ. A successful theorem must use the actual Euler/Artin coefficient structure, not the audited analytic data. |
| R157 | P0, P8 | class-group matrix-tree support amplifier and raw target detector **THEOREM**; determinant/resolvent normalized-conductor gain **CLOSED** | The quotient partial-zeta convolution matrix has spectrum `L_F(s,eta)`, and `det'(zeta_F I-Z)=|C_H| tau` with `tau` a positive spanning-tree polynomial supported beyond `H^(|C_H|-1)`. The raw detector `zeta_F det'` retains the target, but has no constant term; monomial renormalization preserves its zeros and replaces the absolute cutoff by uncontrolled relative norm gaps. Normalizing by the class-field zeta gives exactly `product_(eta!=1)(zeta_F/L_eta-1)`, so the nonlinear gain returns with a collective degree-`|C_H|` mixed numerator and the full `|C_H| log D` ledger. Pseudoinverses discard the target mode, target resolvents restore the undeleted zeta head, and the equal-edge specialization is precisely the R153 scalar root filter. |
| R158 | P0, P8 | conditioned quadratic mixed-point density and deterministic skeleton reduction **THEOREM**; generic family-exceptional strategy **CLOSED** | Growing head signs leave `X^(1-o(1))` characters at `log X asymp H`, while real-character zero density removes denominator zeros with a fixed power saving. Under a retained zeta zero, R155 then forces `zeta-2L_chi` to have a zero for density one of the conditioned family on the full bridge. Unconditioned power-saving mixed-point counts are false by quadratic universality. The exact decomposition `zeta-2L=(E_H-2R_(chi,H))/P_H` reduces target-local selection to the winding and boundary conditioning of `E_H-2`; ordinary AFE/large-sieve moments lose to head entropy, and pointwise anti-concentration has the wrong dimension. |
| R159 | P0, P8 | quadratic functional-equation reflected-response conservation and local-freedom countermodel **THEOREM**; reflection route **CLOSED** | The exact symmetry `F(s)=d^(s-1/2)F(1-s)` quantizes critical-line `F=2` sites at density `O(log d)` but sends off-line `2`-points to moving values. Reflecting the `H^q` response splits off the complete target residue in the old `H`-supported logarithmic derivative; the `d`-supported remainder is target-free. A reflection-invariant paired factor again begins at `H`. Exact functional-equation functions are locally dense in arbitrary right-disc data via a projection whose poles form an `O(log d)` critical-line lattice, so functional symmetry alone cannot rule out the R156 packet. |
| R160 | P0, P8 | finite collective full-cutoff cofactor factorization and Stokes--Hurwitz inevitability **THEOREM**; fixed finite scalar-bank zero-free route **CLOSED** | For fixed even `q`, fixed `J`, and comparable cutoffs `H_j/H->c_j`, the denominator-free collective numerator is exactly `zeta C_H`. No matter how its complex scalar weights depend on `H`, a full endpoint phase turn creates co-maximal waves and a microscopic nonmonomial exponential-polynomial limit. Hurwitz forces an artificial `C_H` zero in every fixed open strip subset for all large cutoffs. This is an asymptotic no-go, not a ban on finite-cutoff weight chambers. Growing independent optional-prime phase controls are outside the finite-slope theorem. |
| R161 | P0, P8 | deterministic mixed-point microcloud, proportional-order signed transform, and cutoff spectral shift **THEOREM**; local skeleton avoidance **CLOSED**; global signed transport **OPEN** | Every sufficiently large truncated-zeta cutoff has a polynomially large simple `E_H=2` microcloud in every fixed open subset of the strip and near any hypothetical off-line zero. Same-height points add coherently at `k asymp log H`, while rightward corridor clouds can dominate. Exact background subtraction realizes zeta multiplicity as a large-product sheet defect but introduces negative background residues and switches off in the small-product phase. Standard PNT precision cannot resolve the remaining sheetwise difference; a genuinely global signed Stokes-corridor estimate is required. |
| R162 | P0, P8 | bounded causal-Laplace continuum control **THEOREM**; continuum causality obstruction **CLOSED**; direct arithmetic transfer **OPEN at this stage** | A nonnegative truncated Poisson profile cancels the past transform `1/lambda` uniformly at high nonreal frequency with `exp(-cN)` error and horizon `O(N)`. Thus `N asymp log H` gives arbitrary fixed-power continuum accuracy at polynomial cutoff. The profile saturates at one and does not itself handle exact prime counts or the varying `1/(log H+v)` density; R163 supplies a different strict-slack repair. |
| R163 | P0, P8 | actual-prime block lift, late-supported strict slack, and random-sign rounding **THEOREM**; local coefficient-shaping gate **CLEARED**; divisor monodromy and collective cofactor **OPEN** | Actual-count normalization on `x^theta` blocks, `theta>7/12`, leaves only `O(H^(theta-sigma)/log H)` kernel variation, and Bergman rounding to one sign per prime costs `O(H^(1/2-sigma)/sqrt(log H))`. A high-degree-only real exponential polynomial has both approximation error and coefficient norm `exp(-cN)` on a sufficiently small filled conjugate-disc pair, so it absorbs the exact logarithmic prime-density weight and targets the exact analytic head. For every fixed nonreal frequency with `0<delta<5/12`, a polynomial tail of actual signed primes cancels the exact prime head by a fixed power. The theorem cannot represent `Log zeta` on a filled contour around a retained zero and gives no zero-free lower bound for the growing collective cofactor. |
| R164 | P0, P8 | explicit local collective-cofactor templates **THEOREM**; finite-dimensional algebraic obstruction **CLOSED** | A strip-holomorphic `g=zm` satisfies `1-(g-1)^2=z exp(1-z)` with a unit multiplier and no extra zero; small opposite exponential perturbations give distinct positive channels on a fixed disc. Polynomial and rational-sphere versions collapse to the extra root. R155 prevents a normal realization on the whole bridge, so composition and arithmetic boundary control remain open. |
| R165 | P0, P8 | nonlinear safe-fibre normality and global-entire gates **THEOREM**; normal and Borel-entire shortcuts **CLOSED** | Every safe fixed-channel family has a nonnormal unit ratio and an unbounded prescribed-value cloud. Borel exponential independence excludes fixed global entire quadratic and mixed-product identities. The cloud is compatible with polynomial growth, leaving only a wild bounded-domain section. |
| R166 | P0, P8 | independent mixed-product `H -> H^q` amplifier and collective cofactor **THEOREM**; scalar fixed-value obstruction **EVADED**; cofactor bound **OPEN** | `P=1-product_j(F_j-1)` has a nonnegative logarithmic derivative supported beyond `H^q` and, for common zeta carriers, exactly one explicit collective cofactor. Individual forced mixed points do not force the product hypersurface. Local distinct-channel templates and the conditional exponent ledger work; global arithmetic zero-freeness does not follow. |
| R167 | P0, P8 | quantitative nonnormal-ratio cloud **THEOREM**; fixed-power-growth contradiction **FALSE** | A power-small cap error and safe cofactor force ratio characteristic `Omega(log H)` and, under logarithmic characteristic, `Omega(log H)` distinct points over every fixed non-cap value. This bill fits the optional-prime polynomial budget and supplies no strip by itself. |
| R168 | P0, P8 | finite-union delayed actual-prime control **THEOREM**; completed left/right cutoff **CLOSED by hull** | Bernstein--Walsh delayed approximation, exact prime-density correction, actual-count lifting, and sign rounding extend to fixed finite separated compacta. On a completed transition contour, the unit disk plus outside arc fills a lens in the polynomial hull, so a fixed nonzero-left/zero-right trace cannot be approximated. Only hull-compatible varying targets, explicit meromorphic divisors, or direct cofactor control survive. |
| R169 | P0, P8 | two-unit normal form and growing-channel ledger **THEOREM**; scalar `q=2` prescribed fibre **OPEN** | Exact divisor equality for the two-channel product is equivalent to `1+G=R_1+R_2` with head-normalized units. Growing even `q` escapes this equation; for `q asymp log H`, support is `exp(c log^2 H)`, algebra size is polynomial in `H`, and propagation costs only fixed-power times polylogarithms. Global algebraic branch and slope conditioning remain. |
| R170 | P0, P8 | high-girth transfer-determinant support amplifier **THEOREM**; divisor-preserving matrix completion **OPEN** | Jacobi's formula expands `P'/P` into positive closed walks, so directed girth `q` forces support beyond `H^q`. Cyclic stochastic layers retain the common target through a simple Perron factor; a one-dominant-slope construction removes the local `O(1/q)` root. Ambient `GL_d` flexibility does not enforce edgewise exact divisors, cap normalization, or the growth ledger. |
| R171 | P0, P8 | normalized scalar-unit and `GL_2` audit **THEOREM**; constant/inner scalar shortcuts **CLOSED**; edgewise matrix lift **OPEN** | A moving omitted value tending to zero is impossible with fixed divisor, and bounded Blaschke prescribed-fibre normalization contradicts R155. An explicit cap-normalized determinant-one `GL_2` split makes the matrix cofactor exactly safe, but forcing its entries to be causal tails factors the output tail across those edges and consumes the claimed support gain; constant edges restore short walks. |
| R172 | P0, P8 | hull-compatible delayed approximation and arbitrary-horizon prime lift **THEOREM**; divisor-preserving completion **OPEN** | Low-degree deletion gives the exact exponent loss `log(R/r)` and a sharp neighborhood-complexity requirement. Fixed-power logarithmic targets force prime horizon `H exp(Theta(H^chi))`, while the local exact-prime errors remain uniform. The remaining high-jet condition is an amplifier-arity versus outer-remainder inequality, not a hull or prime-count obstruction. |
| R173 | P0, P8 | carrier-affine matrix factorization and coefficient-majorant obstruction **THEOREM**; literal R171 missing-constant lift **CLOSED under positive-majorant high jets** | A causal tail impersonating a constant `t` pays majorant `>=tH^(r/2)` and its reciprocal companion pays `t^(-1)H^(-r/2+o(1))`, erasing the formal two-edge gain. Sparse rounding cannot change this deterministic tax. Arbitrary nonperturbative matrix templates and signed cofactor control remain open. |
| R174 | P0, P8 | hull-compatible exact-divisor construction and peak-propagation lower bound **THEOREM**; polynomial two-sided conditioning **CLOSED** | Power-small cap normalization with a fixed retained divisor forces fixed-power logarithmic oscillation and stretched-exponential contour condition number. The fixed-power-logarithm arithmetic realization remains open. |
| R175 | P0, P8 | unique-zero transform and outer-growth conservation **THEOREM**; scalar fixed-finite-divisor Cauchy high-jet route **CLOSED** | The truncated-log transform removes every finite mixed divisor and shifts support to `H^q`, but its forced outer regular germ makes the closing inequality exactly `q>q`. Only growing signed divisor clouds, non-Cauchy localization, or genuinely multichannel phase control survive. |
| R176 | P0, P8 | complete Hardy residue projection and sharp one-sided harmonic-measure law **THEOREM**; abstract Carleman/partial-boundary route **CLOSED** | Right-arc `H^q` decay forces the complementary norm to grow by the exact harmonic-measure exponent; outer functions and nonlinear realizations make the bound sharp. Reflection restores the target only through the linear `H`-supported channel. The remaining harmonic-log-mean estimate must use actual zeta coefficients. |
| R177 | P0, P4 | Jordan/filter faithfulness and novelty audit **THEOREM/CORRECTION**; further detector engineering without arithmetic advantage **CLOSED** | Fixed causal boxes and primitives already detect every zero carrier; the zeta fixed-box coordinate has predecessors R67/R68. Adaptive masks and growing filter order can manufacture misleading apparent gains. Progress requires a coefficient-specific estimate for an existing faithful detector. |
| R178 | P0, P4 | Euler--Jordan boundary tangent, sparse positive Euler surgery, and backward-Poisson factorization **THEOREM/SYNTHETIC MODEL**; generic positive-Euler coercivity **CLOSED** | Positivity, two PNT normalizations, arbitrary finite-head retention, density-one zeta agreement, and arbitrarily accurate divisor identities coexist with planted near-one zeros. Fixed-interior Jordan gain is canceled by pole translation, while positive backward-Poisson deconvolution with weighted tightness is exactly the strip criterion. |
| R179 | P0, P4 | completion-defect and reflection-parity decomposition plus fixed causal-box criterion **THEOREM**; direct completion-defect bridge **CLOSED** | The defect measures the difference of reflected divisor multiplicities, while every paired zero pole lies in the odd/sum channel. For zeta the defect vanishes and the R95/R68 residual is exactly the original carrier. Symmetrization enforces a functional equation while preserving an off-line quartet but loses the ordinary Euler expansion. A fixed logarithmic average is a weaker faithful detector with an absolutely convergent zero formula, but no known power estimate. The open mechanism is therefore a finite coefficient--completion inequality on the odd channel, not defect control. |

| R180 | P0, P4 | uniform-strip to complete-R68 reverse calibration **CONDITIONAL THEOREM/CORRECTION** | A width-\(\delta\) strip gives the endpoint fixed-window field bound and hence the licensed every-scale-block energy and \(X_I\) bound with exponent \(1-2\delta\). Thus the complete fixed-window target is strip-equivalent, not overstrong. Overspecification begins only after separating rectangles/cofactors/sectors or allowing arbitrary cutoffs; those do not inherit the same saving. Reverse propagation remains open for the fixed-step and fixed-total-width single schedules; R80 proves a moving-edge converse for its distinct proportional-order dyadic bank. |
| R181 | P0, P4 | finite completed-source commutator algebra **THEOREM/NO-GO IN SCOPE**; strict S1 adapter **REJECTED** | The arithmetic window commutator either vanishes in an interior prefix, reproduces the original `Lambda` source on a narrow shell, or carries an uncontrolled exterior divisor tail.  The completion-sensitive reflection commutator is off-critical-faithful but equals a multiplier times the reflected original source; finite iteration stays in `M_a+M_bR`.  No fixed-power estimate or strip follows.  Operators outside this finite algebra are not classified.  The bounded next experiment is the single QP/Turan source-fiber bifurcation. |
| R182 | P0, P9 | QP/Turan finite dual algebra **THEOREM** and filled real-node soft model **SYNTHETIC NO-GO**; actual-prime route **PARKED** | A diffuse positive antenna with full-band dip `Y^(-1/2+o(1))` makes the direct synthetic radial radius at most `Y^(-1/2+o(1))` and the event-dependent `v`-fiber support at most `Y^(-.499+o(1))`, despite a legal contiguous event, prime-scale density/gaps, unpaired support, and generic nonresonance. The missing field is literal deterministic prime-log placement at aperture resolution. No actual-prime `LTRAD_P`, DPA, strip, FC, or RH result follows. |
| R183 | P0, P3, P4 | Fredholm-contact exterior factorization classification and backward-strip audit **THEOREM/PROJECT SYNTHESIS**; unrestricted direct factorization **COLLAPSED TO KNC** | The typed charge is `Gamma=P_WG|V_a`. At `A=I+compact`, `Gamma=T A` for some bounded `T` iff KNC. Dense-core equality requires a closable factor; Schur positivity gives only square-root alignment; shifted resolvents expose the null charge as a reciprocal pole. A fixed strip feeds complete R71 and shifted `Psi`, while unshifted universal factorization is an RH amplifier. Only a concrete inverse-free completed-source `T^src` can reopen that direct-RH lane. |
| R184 | P0, P3, P4 | constructive completed-source normal form and compact source derivative **THEOREM/PROJECT SYNTHESIS**; exact global factor **OPEN** | Every finite regular source word leaves the entire collar charge on a contact mode. Suzuki's Lerch source nevertheless has an exact four-root Cauchy representation, with rank-two completion and prime-dilation atoms; its local jump determinant is one, but global exterior solution-independence is exactly KNC. The fixed-window derivative `S_h` is strip-calibrated by a one-sided Landau theorem under a globally fixed sign and sufficient smoothing; the standard causal ramp bridge includes a fixed `-V_delta` collar. Complete R71 remains the strip discovery surface, and no strip or RH follows. |
| R185 | P0, P3, P4 | four-Cauchy exterior-source **EXACT CALCULATION/PROJECT SYNTHESIS**; global compatibility **OPEN**; scalar root--dilation noncommutativity **PARKED** | The fourth-root filter cancels exactly one pole mode per exterior side for arbitrary smooth localized data. The opposite pole, every-fourth higher moments, and prime-power samples survive. Root rotations and positive dilations commute globally; affine reflection relates the readouts; fixed-window compression adds a channel cocycle. The derivation never uses the homogeneous old equation. R186 subsequently closes the split serialization and weak/L2 adapter, without proving total-readout annihilation. No `T^src`, strip, FC, or RH follows. |
| R186 | P0, P3, P4 | split-Carleman/form-domain **EXACT CLOSEOUT**; generic endpoint jets **REFUTED**; coefficient-free propagation **REFUTED**; zeta KNC **OPEN** | The old finite part has exact additive and minimal four-root serializations. Genuine contacts have a weak old equation, bounded `L2` finite-collar readout, and continuous potential plateau. Pointwise/all-jet traces are not continuous on the generic logarithmic form domain. Exact arcsine and rational PSD split-Cauchy/dilation witnesses kill the soft propagation mechanisms, but not completed-zeta KNC. The remaining direct theorem is zeta-specific `E|ker(A_a)=0`; complete R71 remains the primary strip coordinate. No strip, FC, or RH follows. |
| R187 | P4 | completion-preserving additive and Mellin minor-band localization **THEOREM/LOCAL-PREDECESSOR SYNTHESIS**; analytic tails **CLOSED**; completed principal band **OPEN/STRIP-EQUIVALENT** | Earlier local audits already supplied the prime-minus-continuum architecture and principal-band obstruction. R187's delta is the exact frozen Vaughan affine center, explicit order-four `.46/.92/.96` additive-tail ledger, Gevrey Mellin-difference localization, and signed finite falsifier. The hard split is real but not positive. R87 already controls primitive reciprocal kernel response; the remaining `X^(49/50)` theorem is the signed high-determinant accumulation plus the reducible/low-character projector and exact center before absolute values. No audited primary theorem supplies that balanced completed projector-coupled dispersion estimate. No strip, FC, or RH follows. |
| R188 | P4 | exact finite principal-band cofactor serialization **THEOREM/PROJECT SYNTHESIS**; target-matched collar **CLOSED**; completed high-cofactor correlation **OPEN/STRIP-EQUIVALENT** | Writing `gamma=(mu 1_(>U))*(Lambda 1_(>V))` gives a finite nonzero-alias sum plus the exact constant/logarithmic center, with every square cross term retained. Smooth compactification makes `q<=X^(.99)/4` aliases power-small. Fifth-order Fourier decay further gives an exponent-equivalent target collar `q>X^(.998)`, physical quotient `v=n/q<=X^(.002+o(1))`, `|a|<=X^(.0025)`; the discarded field is `X^(.49+o(1))`, at rather than below the target amplitude scale. The surviving `q~X` core still contains determinant one, determinant `~X`, and reducible pairs. An isolated actual semiprime unit-alias block has energy `X/log^2 X`; hence componentwise `X^(.98)` estimates are false, although complementary completed sectors may cancel it. The R105 scalar Wright top box remains `X^(15/8+o(1))`, worse than the direct `X^(1+o(1))` scale. The finite tail is pole-faithful. A joint actual-coefficient, all-alias/all-conductor completed correlation saving `X^(-1/50)` remains the endpoint; no strip, FC, or RH follows. |

## 6. Research-allocation rule

An exact reformulation is a **detector**, not yet a proof engine.  A branch is
promoted only if it supplies a law not already equivalent by definition to
the sign, zero-exclusion, or cancellation being sought.  Before opening a new
branch, record:

1. its canonical family above;
2. its exact quantifiers and arrow direction;
3. the new source of sign, order, compactness, integrality, or cancellation;
4. a countermodel separating that source from the desired conclusion; and
5. whether the proposed estimate is stronger than RH.

As a calibrated endpoint rather than the active bounded experiment, the
cleanest scalar target is P4/R66--R77: bound one fixed-log-width triangular
discrepancy over actual primes, prove its
centered actual-prime geometric Birkhoff sums uniformly bounded, or prove the
equivalent aggregate centered Type-II estimate in R68.  R76--R77 show that
one fixed power saving in the complete fourth moment is exponent-equivalent
to an explicit fixed strip; it is a calibrated endpoint, not an easier
intermediate lemma.  P5/R64 is the exact
compressed quadratic target when polarization is essential.  P3/R62 explains
why a common negative shift cannot evade RH, and P7/R63 is a compact global
spectral restatement but currently has no arithmetic positivity mechanism.
None of these statements supplies the still-missing bound or positivity law.
After R186, the QP/Turan branch remains parked and the unrestricted
exterior-factorization experiment remains exactly KNC at a Fredholm contact.
The explicit four-Cauchy exterior readout has now been calculated: one pole
mode cancels per side, its scalar root--dilation covariance commutes, and
reflection relates the two sides.  R186 serializes the old split finite-part
system and closes its weak/L2 form-domain adapter; generic endpoint jets are
unavailable and the exact soft-mechanism falsifiers fail.  This leaves global
compatibility exactly KNC, not a smaller technical debt.  The active strip discovery coordinate
is the complete source-preserving fixed-window R71 bound; its compact
derivative is the clean scalar falsifier.

## 7. Maintenance rule

Every new registry row must either map to P0--P11, introduce a genuinely new
carrier with both arrows audited, or be labelled as a mechanism, diagnostic,
conditional theorem, or no-go result.  If a later proof changes an arrow, edit
this ledger and the originating registry row in the same change.
